no_skin_list.txt
	This file contains all PNG images that the "Theme color" must NOT be applied to.
	Otherwise colors in the image will be "blend/replaced" with the selected theme color.
	
	
The X offset in the top-bar is hardcoded in the firmware. The skin settings do not apply









Textview properties:

	[Scrolling text if too long for label, but default left]
		"type":"roll|left",
		
	[Right aligned]
		"type":"static|h_right",
		
		